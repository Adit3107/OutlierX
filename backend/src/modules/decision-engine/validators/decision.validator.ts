import {
  DecisionQuerySchema,
  DecisionRecalculateSchema,
  DecisionTestSchema,
} from '@anomaly/shared';

export const decisionQueryValidator = DecisionQuerySchema;
export const decisionRecalculateValidator = DecisionRecalculateSchema;
export const decisionTestValidator = DecisionTestSchema;
